services:
  backend-api:
    build:
      context: ./backend
    ports:
      - "5000:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development

  frontend-app:
    build:
      context: ./frontend
    ports:
      - "5500:80"
    depends_on:
      - backend-api
